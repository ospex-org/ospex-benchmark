# ---- Ospex public read path (live mode) ----
# Base URL of ospex-core-api. Defaults to the production API if unset.
OSPEX_API_URL=
# Supabase project URL + public anon key (read-only), used for the public
# reference tables: current_odds (reference odds), closing_lines (scoring),
# and odds_history first-appearance reads (watch mode's late-detection gate).
# Both are required in live mode.
SUPABASE_URL=
SUPABASE_ANON_KEY=

# ---- Benchmark serving projection (optional; the publisher is off without it) ----
# The scoped `benchmark_writer` role's password. It holds SELECT and INSERT on
# the nine benchmark_* tables and nothing else, and reaches no protocol state.
# With this set, the host is derived from SUPABASE_URL above; without it, the
# publisher is disabled and every write returns `disabled`.
BENCHMARK_WRITER=
# A complete DSN instead, if the host keeps one. Takes precedence over the pair
# above and is passed to the driver unparsed.
BENCHMARK_DB_URL=
# Overrides for a pooler, a non-default database, or a scratch instance.
BENCHMARK_DB_HOST=
BENCHMARK_DB_PORT=
BENCHMARK_DB_NAME=
BENCHMARK_DB_USER=
# The project's CA certificate, as PEM. Supplying it turns on TLS chain
# verification; without it the connection is encrypted but NOT authenticated,
# because the direct endpoint presents a chain Node does not trust by default.
#
# ⚠ It does NOT apply to a BENCHMARK_DB_URL carrying its own SSL parameter —
#   `sslmode=` or the separate `ssl=`. The driver parses those out of the
#   connection string and they override anything supplied beside them, this CA
#   included. The resolver does not interpret what such a parameter MEANS; it
#   asks only whether it is unambiguous:
#
#     no sslmode/ssl in the URL  -> the CA above is used
#     exactly one, non-empty     -> deferred to entirely; the URL is the story,
#                                   whatever the value is
#     empty, or the same one     -> refused as malformed_dsn_ssl; the publisher
#     twice                         stays disabled rather than guess
#
#   Those two refusals are the only shapes that could downgrade quietly: an
#   empty parameter is ignored or falsified depending on which one it is, and a
#   repeated one is last-wins to the driver but first-wins to a naive read.
#   (Note the driver treats sslmode=require as verify-full; `no-verify` is the
#   mode that means encrypt-without-verifying.)
BENCHMARK_DB_CA=

# ---- Campaign store (the runner's own Postgres; local Docker by default) ----
# Connection string for the campaign store. Defaults to the local spike
# instance, and a local target is left alone: TLS is requested only when the
# host is NOT loopback, because a local PostgreSQL has no TLS and asking for it
# fails outright rather than degrading.
#
# Point this at a managed instance and the connection is encrypted
# automatically. To opt out for a non-loopback host that genuinely has no TLS —
# a service name on a container network, say — state it in the URL itself:
#   postgres://user:pw@db:5432/store?sslmode=disable
# An sslmode in the URL always wins and nothing is added alongside it.
#
# ⚠ node-postgres currently treats sslmode=require as verify-full, so against an
#   endpoint whose CA is not in Node's trust store it fails rather than
#   encrypting. `no-verify` is the mode that means encrypt-without-verifying,
#   and it is also what this repo requests by default when no CA is supplied.
STORE_DATABASE_URL=
# The store's CA certificate, as PEM. Supplying it turns on chain verification
# for a remote target; without it the connection is encrypted but NOT
# authenticated.
STORE_DATABASE_CA=

# ---- Provider credentials (preflight + live mode; the dry run needs none) ----
# A missing key produces an explicit credential_missing result for that arm.
# A local gitignored .env in the repo root is loaded automatically at startup;
# real environment variables always take precedence.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# GOOGLE_API_KEY is accepted as an alternative name for the Gemini key.
GEMINI_API_KEY=
XAI_API_KEY=
